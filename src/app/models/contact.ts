export class Contact {
	name: string;
	address: string;
	mobile: string;
	home: string;

	constructor(name: string, address: string, mobile: string, home: string) {
		this.name = name;
		this.address = address;
		this.mobile = mobile;
		this.home = home;
	}
}
